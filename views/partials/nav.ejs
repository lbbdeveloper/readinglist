<nav class="navbar navbar-expand-lg" style="background-color: #d6a400">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <img
        src="/images/book/logo/book-open.svg"
        class="card-img-top nav-logo"
        alt="logo"
      />Reading List</a
    >
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link" href="/book">All Books</a>
        </li>

        <form action="/book/search" method="POST" class="d-flex" role="search">
          <input
            name="search"
            class="form-control me-2"
            type="search"
            placeholder="Search"
            aria-label="Search"
          />
          <button class="nav-link" type="submit">Search</button>
        </form>

        <% if (user) { %>
        <li class="nav-item">
          <a class="nav-link" href="/myreadinglist/<%= user.profile._id %>"
            >My Reading List</a
          >
        </li>
        <% } %>
        <li class="nav-item">
          <% if (user) { %>
          <a class="nav-link" href="/auth/logout">Logout</a>
        </li>
        <% } else { %>
        <li class="nav-item">
          <a class="nav-link" href="/auth/google">Sign-In</a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
